---
name: docker-release

on:   # yamllint disable-line rule:truthy
  release:
    types:
      - 'published'
  push:
    branches:
      - 'main'

jobs:
  build:
    name: Build and push Docker image to GitHub Container registry
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Docker Setup Buildx
        uses: docker/setup-buildx-action@v3.7.1

      - name: Docker Login
        uses: docker/login-action@v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker image reference
        run: |
          cat <<EOF>> $GITHUB_ENV
            REFERENCE=$(echo ${{ github.ref }} | sed -E 's/refs\/(tags|heads)\///')
            META_REFERENCE=${{ startsWith(github.ref, 'refs/tags/') && 'stable' || 'latest' }}
          EOF

      - name: Docker image labels
        run: |
          cat <<EOF>> $GITHUB_ENV
            LABELS=org.opencontainers.image.title=${{ github.event.repository.name }}
            LABELS=${LABELS},org.opencontainers.image.description=${{ github.event.repository.description }}
            LABELS=${LABELS},org.opencontainers.image.url=${{ github.event.repository.html_url }}
            LABELS=${LABELS},org.opencontainers.image.source=${{ github.event.repository.clone_url }}
            LABELS=${LABELS},org.opencontainers.image.version=${{ env.REFERENCE }}
          EOF

      - name: Build and push Docker image 🚢
        uses: docker/build-push-action@v6.9.0
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: ${{ github.repository }}
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.REFERENCE }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.META_REFERENCE }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
